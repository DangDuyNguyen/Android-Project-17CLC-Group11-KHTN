<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ndduy.gamecollection2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Game Collection 2020!"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimalRacingActivity" />
        <activity android:name=".SnakeActivity" />
        <activity android:name=".CardFlipper.CardFlipperActivity" />
        <activity android:name=".CardFlipper.GameActivity" />
        <!--
<receiver android:name=".LobbyFragment$StatusUpdateReceiver">
            <intent-filter>
                <action android:name="status.action.UpdateStatus" />
            </intent-filter>
        </receiver>
        -->
        <service android:name=".UpdateStatus" />
    </application>

</manifest>